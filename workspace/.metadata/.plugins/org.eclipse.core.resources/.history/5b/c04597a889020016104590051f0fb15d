import api.Combiner;

public class LetterCollector implements Combiner {
	public String combine(String first, String second) {
		int[] a = new int[26];
		String s = first;
		String sec = second;
		int i;
		//acquire initial index
		for(i = 0; i < second.length(); i++) {
			
		}
		return s;
	}
}
